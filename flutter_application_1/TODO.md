# TODO - Edit Profile (chưa hoàn thiện)

- [x] Sửa/tạo màn hình `EditProfileScreen` cho phép chỉnh `name` và `avatar/photo`.
- [x] Cắm navigation từ `ProfileScreen` sang `EditProfileScreen`.
- [x] Tích hợp upload ảnh lên Cloudinary (đang có `CloudinaryService`).
- [x] Cập nhật Firestore `users/{uid}` và refresh `AuthProvider` để UI hiển thị dữ liệu mới.
- [ ] Chạy `flutter run` để kiểm tra trực tiếp trên máy.


